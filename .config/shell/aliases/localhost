#
# Bash aliases for Termux
#
# Author: Rafael Cavalcanti


# Source common aliases
. $ALIASES_DIR/base

# Fix aliases that don't work on Busybox
alias rm="rm -v"
alias ln="ln -v"
alias free="free -m"
unalias grep

# Screen is narrow, adapt ll
alias ll="ls -laFh -og --group-directories-first"

# Replace ssh with ssha wrapper, so we use ssh-agent
alias ssh="ssha"

# Browsing
alias zd="cd /sdcard/Download"
alias zs="cd /sdcard"
alias zr="cd $PREFIX"

# Turn off TV via Pi
alias td="ssh pi -t bash -ic td"

# Backup Termux
alias backup="tar \
  -C /data/data/com.termux/files \
  -zcf /sdcard/Sync/Drop/termux-backup-$(date +"%Y-%m-%d_%H-%M-%S").tar.gz \
  home usr"


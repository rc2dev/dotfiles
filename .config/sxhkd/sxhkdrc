super + b
  blueman-manager

super + c
  io.elementary.calculator

super + shift + d
  light-dark

super + e; {h, y}
  { \
    $TERMINAL -e nvim +History, \
    $TERMINAL -e dotfiles cmd nvim +GFiles \
  }

super + f
  $TERMINAL -e ranger

super + shift + f
  nautilus

super + {_, shift, ctrl} + n
  { \
    $TERMINAL -e notes name, \
    $TERMINAL -e notes dump, \
    $TERMINAL -n plan -e notes today, \
  }

super + o
  dmenu_radio

super + p
  keepassxc

super + shift + q
  xkill

super + r
  dmenu_url

super + s; {m, w, y}
 { \
  $TERMINAL -n cmus -e cmus, \
  qimgv $WALLPAPERS, \
  dmenu_yt, \
 }

super + v
  pavucontrol

super + x
  $TERMINAL -e bash -c "\
    fd --follow --exclude 'Cursos/' --exclude 'Notes/' . ~/Documents ~/Downloads $NOTES/resources ~/Videos | \
    sed \"s|$HOME|\~|\" | \
    fzf --height 100% --margin 7%\,5% --no-preview | \
    sed \"s|~|$HOME|\" | \
    xargs -r -d'\n' -P 0 -n 1 xdg-open \
  "

super + {_, shift} + w
  $BROWSER {_, --incognito}

super + z
  $BROWSER https://www.myfitnesspal.com/pt/food/diary

super + {_, shift, ctrl } + period
  dunstctl { context, close, history-pop }

super + Return
  $TERMINAL

alt + Tab
  dswitcher

ctrl + alt + v
  clipmenu

Print
  flameshot gui

Print + shift
  flameshot gui --delay 5000

XF86Calculator
  gnome-calculator


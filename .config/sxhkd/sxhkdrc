# sxhkdrc
#
# Author: Rafael Cavalcanti <https://rafaelc.org/dev>

super + a
  dmenu_orgprod

super + b
  blueman-manager

{super + c, XF86Calculator}
  gnome-calculator

super + {_, shift } + e
  { \
    $TERMINAL -e nvim, \
    thunderbird, \
  }

super + shift + d
  theme-mode toggle

super + f
  firefox-esr

super + {_, shift, ctrl } + n
  dunstctl { \
    close, \
    history-pop, \
    context; dunstctl close \
  }

super + o
  dmenu_document

super + p
  keepassxc

super + shift + q
  xkill

super + r
  $TERMINAL -e lf

super + {_, shift} + s
  { \
    rofi -show ssh, \
    dmenu_tmuxinator \
  }

super + {_, shift} + u
  { \
    dmenu_radio, \
    rhythmbox \
  }

super + v
  pavucontrol

super + w
  $BROWSER

super + y
  mpv --shuffle "https://www.youtube.com/playlist?list=WL"

super + Return
  $TERMINAL

# Arguments:
# -i: unneded with patched dmenu to case insensitve, but necessary using rofi.
# -theme-str: to larger width.
ctrl + alt + v
  clipmenu -i -theme-str "window \{ width: 900px; \}"

ctrl + alt + m
  rofimoji

Print + {_, shift}
  flameshot gui {_, --delay 5000}

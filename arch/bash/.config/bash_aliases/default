#
# Default aliases
#
# Autor: Rafael Cavalcanti

# Source common aliases
. ~/.config/bash_aliases/base

# Arch's Pacman
if [ -e /usr/bin/pacman ]; then
	alias update="sudo pacman -Syu && trizen -Syu --aur --devel --needed \
		&& git -C ~/.dotfiles pull \
		&& vim +PlugUpgrade +PlugUpdate +qa!"

	# Arch's Reflector
	if [ -e /usr/bin/reflector ]; then
		alias pm="sudo reflector -l 5 --sort rate \
			--save /etc/pacman.d/mirrorlist && echo 'OK.' && sudo pacman -Syy"
	# Manjaro's Pacman-Mirrors
	elif [ -e /usr/bin/pacman-mirrors ]; then
		alias pm="sudo pacman-mirrors -f 10"
	fi
fi

# Apt
if [ -e /usr/bin/apt ]; then
	alias update="sudo apt update && sudo apt full-upgrade && sudo apt clean \
		&& sudo apt autoremove \
		&& git -C ~/.dotfiles pull \
		&& vim +PlugUpgrade +PlugUpdate +qa"
fi


#!/usr/bin/env bash
# dmenu_run improved
# - If the command ends with "!", start it in a terminal.
#
# Author: Rafael Cavalcanti - rafaelc.org

readonly cmd="$(dmenu_path | dmenu "$@")"
case $cmd in
    *\! ) $TERMINAL_RUN $(cut -d'!' -f1 <<< $cmd) ;;
    * ) $cmd ;;
esac
